# Curated seed subexpressions for default (non-safe) grammar augmentation.
# Policy:
# - abstract only (A/C, no Xi, no K)
# - avoid simple one-branch saturators and similar shortcut motifs
# - prefer interaction, cancellation, and symmetric repeated-term structure
# - keep seeds compatible with the current supexp validation filter

(A-A)*(A+A)
(A-A)*(A-A)
(A*A-A*A)
(A-A)/(A*A)
((A-A)*(A+A))/(A*C)